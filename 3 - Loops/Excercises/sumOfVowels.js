function solve(text) {
    
    for (i = 0; i < text.length; i++) {
        switch (text) {
            case 'a':
                sum +=
                break;
            case 'e':
                sum += 2;
                break;
            case 'o';
                sum += 4;
                break;


        }
        console.log(sum);
    }
    
}

solve('hello')