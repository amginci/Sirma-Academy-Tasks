let multiplier = 0;

while (multiplier <= 10) {
    console.log('1 x ' + multiplier + " = " + multiplier * 1);
    console.log('2 x ' + multiplier + " = " + multiplier * 2);
    console.log('3 x ' + multiplier + " = " + multiplier * 3);
    console.log('4 x ' + multiplier + " = " + multiplier * 4);
    console.log('5 x ' + multiplier + " = " + multiplier * 5);
    console.log('6 x ' + multiplier + " = " + multiplier * 6);
    console.log('7 x ' + multiplier + " = " + multiplier * 7);
    console.log('8 x ' + multiplier + " = " + multiplier * 8);
    console.log('9 x ' + multiplier + " = " + multiplier * 9);
    console.log('10 x ' + multiplier + " = " + multiplier * 10);

    multiplier++;
}