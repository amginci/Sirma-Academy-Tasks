function mathSolve(number) {
    let oddSum = 0;
    let evenSum = 0;

    while (number > 0) {
        let lastDigit = number % 10;
        if (lastDigit % 2 ==0) {
              
    }
    }

    
}